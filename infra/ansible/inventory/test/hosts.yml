all:
  hosts:
    localhost:
      ansible_connection: local
      edge_controllers:
        - id: test-controller
          trains:
            - id: "{{ train_uuid }}"
              name: Test Train
              type: demo
              mqtt_topic: trains/test-train/status
              config_path: /tmp/edge-controller.conf
              # Add any other required metadata here
