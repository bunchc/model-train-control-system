"""FastAPI router modules for API endpoints.

This package contains FastAPI router modules that define the API endpoints
for the central API application.

Modules:
    config: Configuration management endpoints (/api/config)
    trains: Train control endpoints (/api/trains)
"""
